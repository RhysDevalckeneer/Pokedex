import React from 'react';
import { Text } from 'react-native';
import { SafeAreaViewContainer } from '@components/index';

export default function Tab() {
  return (
    <SafeAreaViewContainer>
      <Text>Tab Home</Text>
    </SafeAreaViewContainer>
  );
}
